@model IEnumerable<Auto.Models.CarModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" asp-route-brandId="@ViewBag.BrandId">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Brand.Name)
            </th>
            <th>
                Логотип
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Year)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Brand.Name)
                </td>
                <td>
                    @if (!string.IsNullOrEmpty(item.LogoPath))
                    {
                        <img src="@item.LogoPath" alt="Логотип" width="100" />
                    }
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.CarModelId" asp-route-brandId="@ViewBag.BrandId">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.CarModelId" asp-route-brandId="@ViewBag.BrandId">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.CarModelId" asp-route-brandId="@ViewBag.BrandId">Delete</a> |
                    <a asp-action="Index" asp-controller="Parts" asp-route-carModelId="@item.CarModelId">Запчасти</a>
                </td>
            </tr>
        }
    </tbody>
</table>