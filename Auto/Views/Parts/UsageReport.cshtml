@model Auto.Models.Part
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <title>Акт об использовании запчасти</title>
    <style>
        body {
            font-family: "Times New Roman", serif;
            margin: 0;
            padding: 15mm;
            width: 210mm;
            height: 297mm;
            box-sizing: border-box;
        }

        .header {
            text-align: center;
            margin-bottom: 20px;
        }

        .title {
            font-size: 16pt;
            text-decoration: underline;
            text-align: center;
            margin: 25px 0;
        }

        .content {
            font-size: 12pt;
            line-height: 1.5;
        }

        .signatures {
            display: flex;
            justify-content: space-between;
            margin-top: 50px;
        }

        .signature {
            width: 45%;
            text-align: center;
        }

        .signature-line {
            border-top: 1px solid black;
            width: 80%;
            margin: 0 auto;
            padding-top: 15px;
        }

        .button {
            position: fixed;
            padding: 10px 15px;
            background: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
            font-size: 14px;
            border-radius: 5px;
            margin: 5px;
            display: block;
        }

            .button.print {
                right: 20px; /* Кнопка "Печать" справа */
                bottom: 20px;
                background: #2196F3;
            }

            .button.back {
                left: 20px; /* Кнопка "Вернуться" слева */
                bottom: 20px;
                background: #f44336;
            }

        /* Стили для печати */
        @@media print {
            .button {
                display: none; /* Скрытие кнопок при печати */
            }

            body {
                padding: 0;
                margin: 0;
                width: auto;
                height: auto;
            }
        }
    </style>
</head>
<body>
    <button class="button print" onclick="printReport()">Печать</button>
    <button class="button back" onclick="goBack()">Вернуться</button>

    <div class="header">
        <div><strong>ООО "АвтоСервис"</strong></div>
        <div>г. Москва, ул. Автозаводская, 10</div>
    </div>

    <div class="title">АКТ об использовании запчасти</div>

    <div class="content">
        <p>Настоящий акт подтверждает, что запчасть:</p>
        <p><strong>Наименование:</strong> @Model.Name</p>
        <p><strong>Количество:</strong> 1</p>
        <p><strong>Дата составления:</strong> @DateTime.Now.ToString("dd.MM.yyyy")</p>

        <p>Запчасть использована по назначению при выполнении ремонтных работ.</p>
    </div>

    <div class="signatures">
        <div class="signature">
            <p>Механик:</p>
            <div class="signature-line"></div>
            <p>_________________________/Иванов И.И./</p>
        </div>
        <div class="signature">
            <p>Директор:</p>
            <div class="signature-line"></div>
            <p>_________________________/Петров П.П./</p>
        </div>
    </div>

    <script type="text/javascript">
        function printReport() {
            window.print();
        }

        function goBack() {
            window.history.back();
        }
    </script>
</body>
</html>
